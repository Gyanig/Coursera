#include<bits/stdc++.h>
using namespace std;

class UF{
	public:
		int UF(int n);				 	// number of objects 0->(n-1)
		void union(int p,int q);		// connect 
		bool connected(int p,int q); 	// recursion till find the end or the q from p
};


int main(){
	
	
	
	return 0;
}
